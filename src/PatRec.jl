module PatRec

using LinearAlgebra
using HTTP
import JSON
using Plots
gr()

include("first.jl")
include("second.jl")

# First practicum.
# henlo()
# binary(;steps=10, ϵ=0.0f0, scale=2)
# l1(;width=10, steps=10, repeats=5)

# Second practicum.
separate()

end
